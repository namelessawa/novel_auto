# Nginx reverse proxy for Novel Agent Backend
# Place in /etc/nginx/sites-available/ and symlink to sites-enabled/

# Add this location block to your existing server {} block:
#
# server {
#     listen 443 ssl;
#     server_name your-domain.com;
#     ...existing config...
#
#     # Novel Agent Backend API
#     location /api/ {
#         proxy_pass http://127.0.0.1:8762/api/;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#
#         # SSE support (disable buffering for streaming)
#         proxy_buffering off;
#         proxy_cache off;
#         proxy_set_header Connection '';
#         chunked_transfer_encoding off;
#
#         # Long timeout for generation requests
#         proxy_read_timeout 300s;
#         proxy_send_timeout 300s;
#     }
# }
