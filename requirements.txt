# Core
python-dotenv>=1.0.0
requests>=2.28.0

# LLM client
openai>=1.59.0
tenacity>=8.0.0
# LLM JSON 输出修复 — 兜底未转义引号 / 字面换行 / 尾随逗号 (v2.34)
json-repair>=0.45.0

# Tokenization
tiktoken>=0.5.0
jieba>=0.42.0

# FastAPI / Tick backend
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
sse-starlette>=2.2.0
pydantic>=2.10.0
httpx>=0.27.0
websockets>=14.0

# Memory / Vector / Graph
# Compatibility lock: 混装旧 protobuf 与新版 OpenTelemetry proto 会在导入
# chromadb 时触发 ``google.protobuf.internal.builder`` ImportError。以下组合已
# 在项目 .venv 验证；同一组升级，禁止只升级其中一个包。
chromadb==1.5.5
protobuf==6.33.5
opentelemetry-api==1.40.0
opentelemetry-sdk==1.40.0
opentelemetry-proto==1.40.0
opentelemetry-exporter-otlp-proto-common==1.40.0
opentelemetry-exporter-otlp-proto-grpc==1.40.0
opentelemetry-semantic-conventions==0.61b0
networkx>=3.4
sentence-transformers>=2.2.0
numpy>=2.2.0
posthog>=3.0.0  # chromadb telemetry shim

# Auth (v2.26 — email-OTP register + optional password login)
# bcrypt<5 — passlib 1.7.4 还不支持 bcrypt 5.0 的 API 变更, 装上会启动期 ValueError。
passlib>=1.7.4
bcrypt>=4.0,<5.0
python-jose[cryptography]>=3.3.0
aiosmtplib>=3.0.1
email-validator>=2.1.0

# v2.33 — 多模态: 分段文本 → 图片 + TTS → 视频
# edge-tts: 开源 (MIT), 走微软 Edge 免费接口, 不需要 GPU / 模型下载
# imageio-ffmpeg: pip 装个静态 ffmpeg 二进制, 跨平台不依赖系统 ffmpeg
# mutagen: 读 mp3 时长 (imageio-ffmpeg 不带 ffprobe, 用 mutagen 替代)
edge-tts>=6.1.0
imageio-ffmpeg>=0.5.0
mutagen>=1.47.0
